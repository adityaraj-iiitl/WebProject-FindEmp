spring.application.name=FindEmp
server.port=${PORT:8080}

# Aiven MySQL Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://${MYSQLHOST:mysql-1d760edd-vasaviinumula24-e902.b.aivencloud.com}:${MYSQLPORT:15601}/${MYSQLDATABASE:defaultdb}?createDatabaseIfNotExist=true&useSSL=true&ssl-mode=REQUIRED}
spring.datasource.username=${MYSQLUSER:avnadmin}
spring.datasource.password=${MYSQLPASSWORD:AVNS_o4KUPoa7TBd5Av8paXQ}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# AI Chatbot Configuration
openrouter.api.key=${OPENROUTER_API_KEY:sk-or-v1-1c4...a3c}
openrouter.referer=${OPENROUTER_REFERER:https://findemp.onrender.com}
gemini.api.key=${GEMINI_API_KEY:}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://web-project-find-emp-aqh5.vercel.app,https://findemp.onrender.com,http://localhost:5173,https://findemp-git-main-vasavi-s-project.vercel.app}
