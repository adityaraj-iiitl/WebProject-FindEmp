spring.application.name=FindEmp
server.port=${PORT:8080}

# Aiven MySQL Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://${MYSQLHOST:mysql-1e2aa5cc-adityarajmn203-1b1f.l.aivencloud.com}:${MYSQLPORT:14903}/${MYSQLDATABASE:defaultdb}?createDatabaseIfNotExist=true&useSSL=true&ssl-mode=REQUIRED}
spring.datasource.username=${MYSQLUSER:avnadmin}
spring.datasource.password=${MYSQLPASSWORD:AVNS_jpPGwU5ov9ttNKdM0zC}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# AI Chatbot Configuration
openrouter.api.key=${OPENROUTER_API_KEY:sk-or-v1-1c4...a3c}
openrouter.referer=${OPENROUTER_REFERER:https://findemp.onrender.com}
gemini.api.key=${GEMINI_API_KEY:}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://findemp.onrender.com}
